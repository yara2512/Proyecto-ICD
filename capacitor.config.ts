import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'practica-3-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
